<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.baseball.domain.players.PlayersDao">

	<insert id="insert">
		INSERT INTO players(teamId, name, position, playerNum, createDate)
		VALUES(#{teamId}, #{name}, #{position}, #{playerNum}, now())
	</insert>
	
	<select id="findAll" resultType="site.metacoding.baseball.domain.players.Players">
		SELECT * FROM players ORDER BY id ASC
	</select>

	<select id="findById" resultType="site.metacoding.baseball.domain.players.Players">
		SELECT * FROM players WHERE id = #{id}
	</select>
	
	<update id="update">
		UPDATE players SET name=#{name}, position=#{position}, playerNum=#{playerNum}
		WHERE id = #{id}
	</update>
	
	<delete id="deleteById">
		DELETE FROM players WHERE id = #{id}
	</delete>

</mapper>